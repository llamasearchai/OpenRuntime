# Core FastAPI framework
fastapi==0.104.0
uvicorn[standard]>=0.24.0

# Data validation and parsing
# Pinned to Pydantic v1 for compatibility with langchain and langchain_openai
pydantic==1.10.14

# HTTP client (pin below 0.28 to maintain compatibility with starlette TestClient)
httpx>=0.25.0,<0.28

# WebSocket support
websockets>=11.0.0

# AI and ML
openai>=1.99.0
openai-agents==0.2.5
langchain
langchain-openai
llm>=0.26.0
llm-ollama>=0.3.0
llm-mlx>=0.1.0; platform_machine=="arm64"
llm-embed-onnx>=0.1.0
onnxruntime>=1.16.0

# Async support
aiofiles>=23.0.0
asyncio-mqtt>=0.11.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# JSON/YAML parsing
pyyaml>=6.0
orjson>=3.9.0

# Validation and security
email-validator>=2.0.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
cryptography>=41.0.0

# Monitoring and metrics
prometheus-client>=0.17.0
psutil>=5.9.0

# Testing and development (for optional features)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Environment management
python-dotenv>=1.0.0

# Caching
cachetools>=5.3.0

# Date/time handling
python-dateutil>=2.8.0

# Shell command utilities
subprocess32>=3.5.4; python_version<"3.0"

# GPU support - Apple Silicon Metal frameworks
mlx>=0.0.6; platform_machine=="arm64"  # Apple MLX Metal framework
torch>=2.0.0; platform_machine=="arm64"  # PyTorch with Metal support
torchvision>=0.15.0; platform_machine=="arm64"
torchaudio>=2.0.0; platform_machine=="arm64"

# GPU support - Universal (CPU fallback)
torch>=2.0.0; platform_machine!="arm64"  # PyTorch CPU version
torchvision>=0.15.0; platform_machine!="arm64"
torchaudio>=2.0.0; platform_machine!="arm64"

# Database (optional)
sqlalchemy>=2.0.0
alembic>=1.12.0

# CLI utilities
click>=8.1.0
rich>=13.0.0
pyyaml>=6.0.0

# Additional ML dependencies
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Performance monitoring
gputil>=1.4.0